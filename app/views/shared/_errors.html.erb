<% if obj.errors.any? %>
    <h2>The <%= obj.class.name.downcase %> could not be created because:</h2>
    <ul>
        <% obj.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
        <% end %>
    </ul>
<% end %>